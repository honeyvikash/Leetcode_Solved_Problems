class Solution {
public:
    bool stoneGame(vector<int>& p) {
        //as the total sum is odd so odd idxs and even idxs sum are not equal 
        //so it all depends on alice that he choose odd or even idxs so he always wins
        return true;
    }
};